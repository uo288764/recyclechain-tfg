# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/recyclechain
spring.datasource.username=recyclechain
spring.datasource.password=recyclechain123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server Configuration
server.port=8080

# JWT Configuration
jwt.secret=TU_CLAVE_SECRETA_MUY_LARGA_Y_SEGURA_PARA_JWT_RECYCLECHAIN_2024_DEBE_SER_AL_MENOS_256_BITS
jwt.expiration=86400000

# Campaign Configuration
campaign.duration.months=2

# Event-based milestones (per month)
milestone.events.tier1=5
milestone.events.tier2=12
milestone.events.tier3=25
milestone.events.tier4=50

# Event-based bonuses
milestone.events.bonus.tier1=1.1
milestone.events.bonus.tier2=1.2
milestone.events.bonus.tier3=1.35
milestone.events.bonus.tier4=1.5

# Weight-based milestones (kg per month)
milestone.weight.tier1=5.0
milestone.weight.tier2=15.0
milestone.weight.tier3=35.0
milestone.weight.tier4=75.0

# Weight-based bonuses
milestone.weight.bonus.tier1=1.05
milestone.weight.bonus.tier2=1.1
milestone.weight.bonus.tier3=1.15
milestone.weight.bonus.tier4=1.25