# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/recyclechain
spring.datasource.username=recyclechain
spring.datasource.password=recyclechain123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server Configuration
server.port=8080

# JWT Configuration
jwt.secret=TU_CLAVE_SECRETA_MUY_LARGA_Y_SEGURA_PARA_JWT_RECYCLECHAIN_2024_DEBE_SER_AL_MENOS_256_BITS
jwt.expiration=86400000